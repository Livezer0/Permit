// Frontend configuration.
//
// By default the app calls the API on the SAME origin it was loaded from
// (e.g. when the backend serves the frontend, or behind one proxy). Leave this
// empty for that case.
//
// Only set a full URL if you run the frontend on a DIFFERENT origin from the
// API, e.g.  window.PTW_API_BASE = "http://localhost:4000";
window.PTW_API_BASE = "";
