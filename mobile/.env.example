# Base URL of the backend API (no trailing slash). Leave empty for same-origin.
# Example (Codespaces forwarded backend):
# VITE_API_BASE=https://glowing-space-robot-4000.app.github.dev
VITE_API_BASE=
