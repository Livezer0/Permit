# Port the API listens on
PORT=4000
# Allowed CORS origin for the frontend (use the frontend's URL in production)
CORS_ORIGIN=*
# Optional overrides
# PTW_DATA_DIR=./data
# PTW_DB=./data/ptw.db
# PTW_FRONTEND_DIR=../frontend   # static frontend the API also serves
